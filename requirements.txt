# Core dependencies (pure Python stdlib)
# No external dependencies required for core protocol and tunnel!

# Optional dependencies for WDA client
requests>=2.28.0

# Optional for screenshot processing
Pillow>=9.0.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
